node_modules/
dist/
build/
.turbo/
# Next's build output, INCLUDING its Turbopack cache. Not merely noise: the
# cache inlines values from .env.local, so a committed .next/ leaks whatever
# credentials the build could see.
.next/
*.tsbuildinfo
.env
.env.*
!.env.example
.DS_Store
coverage/
bakeoff/**/scorecard*.json
pnpm-lock.yaml.bak
apps/web/mcp.config.json
